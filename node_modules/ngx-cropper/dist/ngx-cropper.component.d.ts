import { OnInit, AfterViewInit } from '@angular/core';
import { NgxCropperService } from './ngx-cropper.service';
import { NgxCropperOption } from './ngx-cropper.model';
export declare class NgxCropperComponent implements OnInit, AfterViewInit {
    private ngxCropperService;
    isShow: boolean;
    viewConfig: NgxCropperOption;
    private config;
    private returnData;
    private fileName;
    private fileType;
    private dom;
    private cropper;
    constructor(ngxCropperService: NgxCropperService);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    /**
     * click apply event
     *
     * @returns
     * @memberof NgxCropperComponent
     */
    onApply(): void;
    /**
     * Hidden edit modal
     *
     * @memberof NgxCropperComponent
     */
    onCancel(): void;
    /**
     * transfer uri to blob
     *
     * @private
     * @param {*} dataURI
     * @returns
     * @memberof NgxCropperComponent
     */
    private dataURItoBlob(dataURI);
    /**
     * init cropper plugin
     *
     * @private
     * @memberof NgxCropperComponent
     */
    private initCropper();
}
